# 회고 — 광전 효과 (고1 물리, evidence 프로파일)

## 게이트 이력
- 1차 빌드→게이트: `FAIL` 13/17. S7(mustInclude/misconceptions 반영 미흡), B2(SVG 선에 채움 전용 클래스 `v-muted`/`v-warn` 사용 + 라벨-선 관통), B5(concept 삽화 viewBox 비율이 칸과 안 맞아 작게 보임), B6(지도안 4쪽로 초과, 마지막 쪽 17%만 채움).
  - 원인: (1) mustInclude/misconceptions 문구를 학생 화면에 원문 낱말 그대로 담지 않고 의역만 해서 낱말 일치율이 70% 미만이었음. (2) `design.md`의 "선에는 -s 클래스만" 규칙을 지키지 않고 채움 전용 클래스(v-muted, v-warn, v-good)를 line/path의 stroke에 씀. (3) 지도안 flow를 9행으로 너무 잘게 나눠 A4 3쪽 한도를 넘김.
  - 고친 방법: timeline 제목을 mustInclude 문장에 가깝게 바꾸고 hook·compare notes에 misconceptions 원문을 그대로 심음(S7 통과). 모든 SVG의 선을 v-ink-s/v-accent-s로 바꾸고 체크/×표시 아이콘은 삭제해 텍스트 라벨로 대체(B2 통과). concept#10 삽화 viewBox를 내용에 맞춰 재설계(B5 통과). 지도안 flow를 9행→6행으로 병합(B6 통과).
  - 2차 빌드→게이트: `FAIL` 15/17. 잔여: B2-CROSS(슬라이드7 "방출 없음" 라벨이 빛줄기와 겹침), B5-VISUAL(concept#10 삽화가 여전히 28%로 30% 미만).
  - 고친 방법: 라벨 위치를 빛줄기 범위 밖(패널 오른쪽 위)으로 옮기고, concept#10 viewBox를 그림 bbox+8% 여백에 딱 맞게 재조정(막대·원 크기 확대).
  - 3차 빌드→게이트: `ALL PASS` 17/17.
- 다음에 처음부터 다르게 할 것: SVG를 작성할 때부터 선 클래스는 `-s`/`v-line`만 쓰기로 미리 체크리스트화하고, 라벨을 넣기 전에 광선/막대가 지나가는 좌표 범위를 먼저 계산해서 겹치지 않는 위치에 배치한다. concept 삽화는 처음부터 viewBox를 컨텐츠 bbox+여백으로 맞춰서 시작하면 B5 재작업을 줄일 수 있다.

## 2회차 (평가자 피드백 반영 — 새 게이트 E1-AXIS·E1-SPOILER·S1-WS-FIGEMPTY·S1-MATCH-ID 대응)
- 요청 사항: ① 활동지 그래프 문항에 `visual`이 실제로 렌더되는지·축 이름 확인 ② 슬라이드11 그래프에 축 이름 추가, f₀ 미만은 점선 외삽으로, "기울기가 빛의 색과 무관" 표현을 "기울기(플랑크 상수)는 금속 종류와 무관"으로 정정(활동지 OX·정답지도 함께) ③ 스포일러 금지 — timeline(슬라이드4)이 판정(compare 다음 장) 전에 1905년 아인슈타인의 설명을 먼저 밝히지 않게 재배치 ④ 슬라이드10 광자 그림을 롤리팝/뉴턴의 요람 은유 대신 금속판에 광자가 부딪혀 전자가 튀어나오는 그림으로 재설계, 슬라이드14 3단계의 추상 아이콘 교체 ⑤ 활동지 선 잇기 항등 매칭 제거, tip을 합쇼체로, Q7"세 문장"과 나가기 질문 문장 수 통일.
- 1차 빌드→게이트: `FAIL` 16/17 — `B6-FILL`(활동지·정답지 1쪽 채움 38% < 62%, "그래프로 확인하기" 섹션 전체가 통째로 2쪽으로 밀림).
  - 시도 1: xLabel/yLabel을 슬라이드11과 동일하게 긴 문구("진동수 f", "광전자 최대 운동 에너지 Eₖ")로 넣었더니 **인쇄용 figure에서 x축 라벨이 페이지 오른쪽 밖으로 잘려 나가는 렌더링 버그**를 발견(스크린 슬라이드에서는 안 잘림 — 컨테이너의 overflow 처리 차이로 추정, `visuals.mjs`의 라벨 위치가 축 끝에서 고정 오프셋이라 여백이 항상 부족함). → 활동지 쪽 라벨만 "Eₖ"처럼 짧게 줄여 해결(슬라이드는 화면 폭이 넉넉해 긴 라벨 유지).
  - 시도 2: figure `height`를 60→42mm로 줄여 페이지 채움을 맞추려 했으나, **그래프가 작아질수록 라인 라벨("Eₖ = hf − W")의 절대 픽셀 오프셋이 상대적으로 커져 이번엔 그 라벨이 잘리는 새 문제**가 생김 — 이 스크립트의 라벨 여백 상수는 그래프 크기에 반비례해 상대 오차가 커지는 구조라 확인.
  - 최종 해결: figure `height`는 60mm로 되돌리고, 대신 "01 개념 정리" 섹션에 짧은 OX 1문항 + 객관식 1문항을 추가해 1쪽 자체의 채움을 늘림(그래프 섹션은 그대로 2쪽에서 시작해도 게이트 통과 조건 충족).
  - 2차 빌드→게이트: `ALL PASS` 17/17.
- 스킬 버그로 보고할 점: `visuals.mjs`의 `plot()`이 xLabel/축 화살표 라벨을 `sx(x1)+22`(가운데 정렬)에 고정 배치하는데, 이 오프셋이 `pad`(60)보다 항상 크고 텍스트 절반 폭(≈40~60px)까지 더해지면 뷰박스 폭과 무관하게 라벨이 항상 오른쪽으로 넘친다. 인쇄용 A4 문서(figure)는 이 초과분을 그대로 잘라내는 반면 화면용 슬라이드는 잘리지 않아, 같은 lesson.json 값이라도 컨텍스트에 따라 렌더 결과가 달라진다 — 축 이름 길이를 문서/화면별로 다르게 쓰는 것으로 우회했지만, 근본 수정은 스크립트 쪽(라벨 오프셋을 뷰박스 안으로 clamp)에서 필요해 보인다.

## 도메인 정확성 검토 — 1회차(작성자 자신, 참고용)
| 슬라이드/문항 | 그림·자료의 물리적 주장 | 판정 |
|---|---|---|
| 슬라이드 1 (표지, 생성 이미지) | 자외선 램프가 금속판을 비추는 분위기 사진(정확한 값·라벨 없는 훅용) | 해당 없음(사실 주장 없음) — 표지 용도로 적절 |
| 슬라이드 3 (hook) | 약한 자외선(고진동수)은 광전자를 방출시키지만 강한 빨간빛(저진동수)은 방출시키지 못한다 | 맞음 — 광전 효과의 대표적 관찰(진동수가 방출 여부를 결정, 세기 무관)과 일치 |
| 슬라이드 5 (concept, 정의) | 금속 표면에 빛을 비추면 광전자가 튀어나온다(광전 효과의 정의) | 맞음 — 표준 정의 |
| 슬라이드 6 (compare) | 파동설: 세고 오래 비추면 어떤 진동수든 방출, 약하면 지연, 세기가 셀수록 전자 에너지 증가 / 입자설: 문턱 진동수 미만은 불가, 이상이면 즉시, 에너지는 진동수에 비례 | 맞음 — 역사적으로 실제 파동설이 내놓았던 예측과 아인슈타인 광양자설의 예측을 정확히 대비시킴(둘 다 "예측"으로만 표기해 사실처럼 서술하지 않음) |
| 슬라이드 7 (diagram, 판정) | 약한 자외선은 즉시 방출, 강한 빨간빛은 방출 없음 — 진동수가 문턱값을 넘어야 함 | 맞음 — 레나르트·밀리컨 실험 결과와 일치, 세기와 무관하다는 핵심 사실 정확 |
| 슬라이드 10 (concept, 광자) | 빛은 이어지는 파동이 아니라 hf 에너지를 가진 광자(낱개 알갱이)의 흐름이다 | 맞음 — 광양자설의 핵심 주장을 정확히 표현 |
| 슬라이드 11 (diagram, 그래프) | $E_k=hf-W$ 직선, 기울기는 진동수와 무관하게 일정(플랑크 상수), x절편 $f_0$가 문턱 진동수, 그 아래에서는 방출 안 됨 | 맞음 — 아인슈타인의 광전 방정식과 정확히 일치. 직선을 x절편 왼쪽(음의 y)까지 연장해 그린 것은 일반적 교과서 관행(수학적 외삽이며, 캡션이 "이보다 진동수가 낮으면 방출되지 않는다"고 명시해 오해 소지 없음) |
| 슬라이드 12 (compare, 세기 vs 진동수) | 세기↑→전류(방출 전자 수)↑, 에너지는 그대로 / 진동수↑→전자 하나의 최대운동에너지↑, 방출 여부 자체를 결정 | 맞음 — 광전 효과의 핵심 구분(세기=광자 개수, 진동수=광자 에너지)과 정확히 일치 |
| 슬라이드 14 (steps 아이콘) | 그래프 직선 확인 → 가로축 교점 찾기 → 진동수 비교 절차 아이콘(추상 도형) | 해당 없음(절차 안내용 추상 아이콘, 구체적 수치·물리 주장 없음) |
| 활동지 문항 3 (표, 파동설 vs 실제) | 전자 방출 시작(서서히 vs 즉시), 세기의 영향(에너지 증가 vs 전류만 증가), 저진동수(결국 방출 vs 방출 안 됨) | 맞음 — 정답지의 세 행 모두 표준적으로 알려진 광전 효과의 파동설-실측 대비와 일치 |

결론: 슬라이드·삽화·활동지에 등장하는 모든 물리적 주장(문턱 진동수의 존재, 세기·진동수의 서로 다른 역할, 즉시성, $E_k=hf-W$ 그래프의 기울기·절편 의미)이 광전 효과의 표준 설명과 일치한다. 오류나 애매한 항목 없음.

## 도메인 정확성 검토 — 2회차(필수, 별도 서브에이전트가 수행 — SKILL.md 7단계 규정)
작성자와 다른 컨텍스트의 verifier 서브에이전트(Sonnet, 읽기 전용)에게 `lesson.json` 전체와 `out/shots/`의 슬라이드 1~17·활동지 1~4쪽·정답지 1~4쪽·지도안 1쪽을 모두 넘겨 독립적으로 검토하게 했다. 특히 (1) 자외선/빨간빛 방출 방향, (2) Eₖ–f 그래프의 실선/점선과 기울기·절편 의미, (3) 타임라인의 스포일러 여부를 중점적으로 다시 확인하도록 지시했다.

| 위치 | 주장 | 판정 | 비고(불일치) |
|---|---|---|---|
| slide 3 (hook) | 약한 자외선(고진동수)=방출 암시, 강한 빨간빛(저진동수)=방출 안 됨 암시 | 맞음 | 캡션과 일치. 훅 단계라 방출 여부를 확정 표시하지 않은 것도 적절 |
| slide 4 (timeline) | 1887 헤르츠→1899 톰슨→1902 레나르트, 미해결 상태로 종결 | 맞음 | "설명했다/해결했다" 등 조기 스포일러 없음. 1905년 아인슈타인 언급은 슬라이드 9~10으로 적절히 지연 |
| slide 5 (concept) | 광전 효과 정의 그림 | 맞음 | 캡션과 일치 |
| slide 7 (diagram, 판정) | 약한 자외선=즉시 방출, 강한 빨간빛=방출 없음 | 맞음 | 세기 아닌 진동수가 결정한다는 방향이 정확, 캡션과 일치 |
| slide 10 (concept, 광자) | hf 에너지의 이산 광자(마름모 4개)가 금속에 도달, 광전자 즉시 방출 | 맞음 | 이산 양자 표현이 캡션과 일치(롤리팝/뉴턴의 요람식 은유에서 벗어남 확인) |
| slide 11 · 활동지/정답지 p2 (Eₖ–f 그래프) | 기울기=h(전 금속 공통), f₀ 이상 실선, 이하 점선 외삽, x절편=f₀ | 맞음 | 부호·구분·캡션 모두 정확. (사소) x축 눈금 "3"이 f₀ 라벨로 가려짐 — 오류 아님 |
| slide 14 (steps 아이콘) | 그래프 판독 절차 픽토그램 | 맞음(단순화) | 절차 설명용이라 문제 없음 |
| 활동지/정답지 p3 (선 잇기) | 광전효과·광자·문턱진동수·광전자 정의 매칭 | 맞음 | JSON 정답([[1,3],[2,4],[3,2],[4,1]]) 의미상 정확, 정답지 연결선도 항등 매칭 없이 교차 |
| 활동지/정답지 p1·p3 (OX·객관식) | 세기↑→기울기 불변, f>f₀→즉시 방출, 기울기(h)는 금속 무관 항상 같음, 광자 에너지 무작위 아님 | 맞음 | 정답지 체크가 모두 물리적으로 올바른 선택지를 지시 |

종합 결론(서브에이전트): 검토한 모든 도표·그래프·삽화·매칭·OX/객관식 정답에서 물리적 오류나 그림-캡션 불일치를 발견하지 못했다. 특히 자주 틀리는 세 지점(자외선/빨간빛 방출 방향, Eₖ–f 그래프의 실선/점선·기울기·절편, 타임라인의 스포일러 여부)이 모두 정확히 구현됨. 이상 없음.
