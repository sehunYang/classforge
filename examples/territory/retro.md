# 회고 — 우리나라의 위치와 영역 (사회 5-1, 2/8차시)

## 7차 라운드 (지도색 산티넬 반영 + 도메인 검토 지적 내용 수정)
- **지도색**: 스킬(diagrams.mjs RENDER_VERSION 8)에 `cfland`/`cfsea`/`cfsky` 산티넬이 생겼다. 그래서 땅은 `v-land`(초록, 해안선은 CSS가 그림), 바다는 `v-sea`(파랑), 하늘은 `v-sky`로 바꿨다. 표지 지구본도 바다색이다. 손으로 넣던 `\pgfsetfillopacity{1}`은 스킬이 자동으로 넣으므로 뺐다. 렌더링에서 초록·파랑을 확인한 뒤, 활동지 문항 3의 "영토는 초록색, 영해는 파란색" 문구를 되살렸다.
- **무해 통항**: 11번 compare와 14번 퀴즈가 "허락 없이 배가 못 지나감"으로 법을 과장하고 있었다. 11번은 "우리 바다를 함부로 이용 못 함"으로 바꾸고, 그림의 화살표를 무해 통항이 없는 비행기의 길(라벨 "비행기")로 바꿨다. 왼쪽은 한반도 위를 가로지르고, 오른쪽은 경계에서 막힌다. 14번은 "영해에서 고기를 잡으려면?"으로 바꾸고, 정답을 "우리나라의 허락 없이는 잡을 수 없다" 하나만 옳게 선택지를 새로 썼다. 두 슬라이드 노트에 무해 통항 참고를 넣었다.
- **기준선**: f2·용어 정의·10번 노트에 통상 기선(썰물 때 해안선)과 직선 기선(서·남해안 바깥 섬을 이은 직선), 대한 해협 3해리를 적었다.
- **12해리 안내선**: 활동지 백지도에 영해 경계 점선을 넣고(정답 그림과 같은 선), 문항에 "해안선에서 점선까지(해안선을 따라 단순하게 그렸어요. 서·남해안은 바깥 섬을 이은 기준선 때문에 실제 영해가 더 넓어요)"를 적었다.
- **새 검토자 지적 반영**: 11번 compare 두 칸 모두 울릉도·독도가 빠져 있었다(틀림 판정). 섬 넷을 다 그리고, 오른쪽 칸에 섬 영해도 칠했다. 비행기 길은 섬 영해를 비켜 남동쪽에서 부산 앞 경계로 오게 바꿨다. 영해 띠는 0.7~0.9°였는데, 그러면 울릉도·독도 영해가 한 덩어리로 붙어 보였다. 그래서 모든 그림에서 0.3°(실제 12해리의 1.5배)로 줄이고, 섬 영해는 실제에 가까운 섬 크기에서 재어 두 섬의 영해가 떨어져 보이게 했다. 12번 영공 아치는 영해 밖과 국경 너머까지 덮고 있었다. 이를 비스듬히 본 지도(세로 0.45배) 위에 영토+영해를 그대로 위로 올린 하늘 기둥으로 바꿨다. 5번 노트의 "각각 다른 색으로"는 그림과 달라 "차례로 짚어가며"로 고쳤다.
- **영공 높이**: 지도안 예상 질문 답을 "국제적으로 정해져 있지 않고 보통 대기권까지(우주는 영공이 아님)"로 바꿨다. 처음 쓴 긴 답은 지도안을 4쪽으로 늘려 B6-PAGES에 걸려서 줄였다.

## 6차 라운드 (손그림 SVG → TikZ 도해 이전, V1-RAWSVG)
손그림 `<svg>` 13개(슬라이드 1·3·5·6·10·11 좌우·12 네 단계, 활동지 백지도 base/answer)를 모두 저수준 `{tikz}`로 바꿨다. 좌표는 손으로 적지 않고 `mapgen.mjs`가 간이 해안선 경위도 표(약 60점)에서 계산한다. 투영은 x=(경도−128)·cos38°, y=위도−38이다. 예전 지도는 가로로 약 1.8배 늘어나 있었고, 북쪽 끝도 42.4°N까지만 그려졌다. 이제 북쪽 끝(유원진)은 43°, 마라도는 33° 바로 위, 마안도는 124° 바로 안쪽, 독도는 132°에 닿는다.
- **헬퍼를 안 쓴 이유**: `geometry`는 꼭짓점마다 점을 찍고 채움이 없어 지도에 맞지 않는다. 지도·단면·아이콘은 채움 다각형이 필요해 `{tikz}`로 그렸다.
- **색 타협**: 도해 엔진은 cfaccent·cfink·cfmuted·cfsoft 네 색만 테마 클래스로 바꾼다. 그래서 `v-land`/`v-sea`/`v-sky` 지도색을 낼 수 없다. 대신 땅은 cfsoft 채움에 진한 해안선, 바다는 cfmuted 30% 반투명, 하늘은 cfink 6% 반투명으로 칠했다. 활동지 문항 3의 "영토는 초록색, 영해는 파란색"은 정답 그림과 색이 어긋나므로 "서로 다른 색으로"로 바꿨다(model 문구도 같이).
- **내용 보정**: 5번 격자의 라벨 "위도/경도"를 선 이름 "위선/경선"으로 바꿨다(4번 낱말 정의와 캡션에 맞춤). 11번 compare의 육각형 땅은 한반도 외곽선으로 바꿨다. 활동지 정답 그림의 큰 사각형 바다는 해안선을 따라가는 영해 띠(0.35°, 실제 12해리≈0.2°보다 조금 과장)로 바꿨다. 이 평면 지도에서는 영공을 그릴 수 없어, 영해보다 크게 그려져 있던 점선 사각형 "영공"은 뺐다(문항은 영토·영해만 묻는다). 10번 단면의 설명 없는 주황 띠도 뺐다.
- **도메인 검토에서 고친 것**: 12번 2·3단계에 섬(제주·마라도, 울릉도·독도) 영해 선을 추가해 4단계와 이어지게 했다. 영해 띠는 압록강·두만강 하구에서 0으로 줄어들어 국경 너머로 나가지 않게 했다.
- **남은 검토 지적(이번 이전 범위 밖, 글 내용)**: 11번·14번의 "허락 없이 배가 못 지나감"은 무해통항 기준으로는 과장이다. 교과서 표현은 "함부로 들어올 수 없다"이다. 기준선=해안선 정의에는 서·남해 직선기선이 빠져 있다. 백지도에 12해리 안내선이 없어 영해를 얼마나 넓게 칠할지 알 수 없다.
- **도메인 검토 요약**(별도 검토 서브에이전트, review.json):

| 슬라이드 | 그림이 주장하는 것 | 글이 주장하는 것 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | 정사영 지구본, 주황 점이 북위 30~60° 사이·가운데 경선 동쪽 | 위도·경도로 위치, 영역 확인 | 맞음 | 점 = 37.5°N 127°E 정사영 좌표 |
| 3 | 흐린 원 '어디쯤일까?' vs 격자 교점 '여기예요!' | 정확한 위치는 위도·경도로 | 맞음 | 대비가 글과 일치 |
| 5 | 가로 실선 '위선', 세로 실선 '경선', 교점 '우리나라' | 위선·경선이 만나는 곳 | 맞음 | 4번 낱말과 일치 |
| 6 | 43°/33°/124°/132° 점선 안에 한반도·섬, 끝점이 선에 닿음 | 북위 33°~43°, 동경 124°~132° | 맞음 | 좌표 실측 대조, 해안선 형태 정확 |
| 10 | 사다리꼴 영토, 양옆 영해, 영토+영해 폭의 영공 돔 | 땅·옆 바다·위 하늘 | 맞음 | 폭·위치 일치 |
| 11 | 왼쪽 배가 해안을 따라 통과 / 오른쪽 영해 띠 경계에서 X | 허락 없이 배·비행기 못 지나감 | 애매 | 그림과 글은 일치하나 배는 무해통항 가능 |
| 12 | 영토 → 점선 영해(섬 포함) → 돔 → 모두 합침 | 영역 찾는 4단계 | 맞음 | 1→4단계 영해 구역이 같게 이어짐 |
| 14 | (없음) | 다른 나라 배는 허락을 받아야 지나감 | 애매 | 무해통항권, 오답 ③이 부분적으로 참 |
| 활동지 3 | 백지도 / 정답: 베이지 영토 + 회색 영해 띠(섬 포함, 국경엔 없음) | 서로 다른 색으로 영토·영해 색칠 | 맞음 | 외곽선 동일, 라벨 위치 맞음 |

(그 밖의 슬라이드는 그림 없음·맞음.)
- **엔진 주의(스킬 보고)**: 시스템 LaTeX(dvisvgm) 경로에서 `fill opacity`가 뒤따르는 도형에 새어 나간다. 그래서 반투명 채움 뒤마다 `\pgfsetfillopacity{1}`을 넣었다.

- **걸린 게이트**: S2-TOTAL FAIL (5번 concept 134자, 11번 compare 164자, 둘 다 125자 예산 초과).
  **원인**: 개념 설명(`t`)과 compare 항목을 완결된 문장형으로 길게 씀 ("적도에서 북쪽·남쪽으로 갈수록 숫자가 커져요" 등).
  **고친 방법**: 항목을 짧은 명사구/축약형으로 바꿈 ("적도에서 멀어질수록", "바다는 아무나 지나감" 등), compare label도 단축. 2회 build→gate 반복 후 ALL PASS.
- **육안 검수에서 고친 것**: vocab 슬라이드(9번, 영토·영해·영공 3개 용어)가 2×2 그리드 레이아웃에서 오른쪽 아래 칸이 비어 한쪽으로 쏠린 빈 공간이 생김. 관련 용어 "기준선"(영해 12해리의 기준이 되는 해안선)을 4번째로 추가해 그리드를 채움 — 내용도 f2(영해 12해리) 개념과 자연스럽게 연결됨.
- **다음엔 처음부터 다르게 할 것**: (1) concept·compare 항목은 처음부터 명사형으로 짧게 쓰고 부연은 notes로 돌린다. (2) vocab을 2열 그리드로 쓸 때는 처음부터 용어 수를 2개 또는 4개로 맞추고 3개는 피한다(빈 칸 발생).

## 스킬 관련 참고사항 (버그는 아니지만 보고)
- (1차 회고) `gate.mjs` S5-TITLE(문체 경고)은 `title`이 있고 물음표로 끝나는 모든 슬라이드에서 `type`이 `hook`/`quiz`/`exit`가 아니면 경고를 냈었습니다. `references/design.md`는 cover도 질문형 제목을 허용하는데 gate는 cover를 예외로 넣지 않아 불일치가 있었습니다 — **2차 라운드에서 스킬이 업데이트되어 S5가 더 이상 cover 질문형 제목에 경고를 내지 않음을 확인**했습니다. 해결됨.

## 5차 라운드 추가 반영 (지도 전용 색 클래스로 통일)
`v-land`(연한 초록)/`v-sea`(연한 파랑)/`v-sky`, `v-land-s`/`v-sea-s`가 라벨 대비까지 검증된 정식 클래스임을 확인함. 슬라이드10(영토·영해·영공 단면도)의 하늘·바다·땅·해안선을 교과 강조색 기반 임시 배색(`v-soft`/`v-paper3`/`v-mid`/`v-ink-s`)에서 지도 전용 클래스로 교체하고, 활동지 백지도(`base`)의 외곽선 색도 `v-ink-s`→`v-land-s`로 통일. 1회 build→gate로 ALL PASS 유지. 이제 슬라이드6·10·12와 활동지 지도가 모두 같은 초록(땅)·파랑(바다) 배색을 쓴다.

## 5차 라운드 (검수자 2차: ACCEPT with conditions, 신규 게이트 B2-CROSS)
새 게이트 B2-CROSS(라벨을 선이 관통하면 실패)와 지도 전용 색 클래스(`v-land`/`v-sea`/`v-sky`, `v-land-s`/`v-sea-s`)가 스킬에 추가됨. 1회 build→gate로 ALL PASS.
- **라벨-선 교차 3건**: 슬라이드3 "여기예요!"(점선 격자의 세로선이 끝까지 내려와 라벨을 관통) → 세로선 길이를 라벨 위에서 멈추도록 줄임. 슬라이드5 "우리나라"(격자 세로선이 라벨을 정확히 관통) → 라벨을 격자 바깥(맨 위 여백)으로 올림. 슬라이드6 "북위 43°/33°" → 가로 점선에서 더 멀리 띄움(내가 이전 라운드에 둔 간격이 글자 높이를 고려 못해 부족했음).
- **정답지 지도 색**: 문항이 "영토=초록/영해=파랑"이라고 했는데 정답 그림은 교과 강조색(주황)을 쓰고 있었음 → 새로 생긴 `v-land`(초록)/`v-sea`(파랑) 지도 전용 클래스로 교체해 문항 문구와 그림이 일치하게 됨. "영공" 라벨도 바깥 점선 경계선과 겹쳐 있어 여백을 늘려 위로 뺌.
- **슬라이드12 절차 아이콘**: 4단계 모두 육각형 덩어리였던 것을 슬라이드6·활동지에 쓰는 것과 같은 간이 한반도 외곽선(`v-land`)으로 통일 교체.

## 4차 라운드 (독립 검수자 REJECT 반영)
검수자가 지적한 5가지를 모두 고침, 1회 build→gate로 ALL PASS.
1. **지도**: 슬라이드 6과 활동지 문항3(백지도 색칠)에 쓰던 육각형을 없애고, 위도 33°~43°·경도 124°~132° 범위 안에 경위도 좌표점 18개를 직접 잡아 이은 한반도 간이 외곽선(+ 제주도·울릉도·독도 작은 도형)으로 새로 그림. 활동지 문항3에 `base`(외곽선만 있는 백지도)와 `answer`(영토=강조색, 영해=띠, 영공=바깥 점선 표시한 채색본)를 추가해 새 게이트 S1-WS-BASE 통과. 표지는 지구본+점으로 위치를 표시하는 추상적 그림이라 "지도를 도형으로 대신"하는 경우가 아니라고 판단해 손대지 않음.
2. **개념 오류**: 위도·경도는 각도, 위선·경선은 그 값이 같은 곳을 이은 선이라는 것으로 슬라이드 4(용어 2개→4개로 위선·경선 추가), 슬라이드 5 개념 포인트·캡션·노트, 활동지 개념 상자 빈칸 설명, 지도안의 "위도는 가로선…" 메모와 학생 활동 문구까지 전부 수정.
3. **단면도**: 슬라이드 10은 영해를 영토 아래 하나의 띠로 그렸던 것을 영토 좌우에 걸친 두 개의 띠로 바꿔 "영토 옆 바다"로 고침. 슬라이드 12의 3단계(영공 표시) 아이콘은 하늘만 따로 떠 있던 것을, 2단계와 같은 영해 점선 타원+영토 도형 위에 하늘이 바로 얹히도록 다시 그림.
4. **활동지 정답 유출**: 개념 정리 상자에 있던 "우리나라는 북위 33°~43°, 동경 124°~132° 사이에 있다" 문장을 삭제(문항1·2의 정답을 미리 알려주고 있었음).
5. **라벨 겹침**: 슬라이드 6의 "북위 43°"/"북위 33°" 라벨을 점선에서 더 멀리 떨어뜨림(43°는 위로, 33°는 아래로 이동).

## 2차 라운드 (스킬 업데이트 반영)
- **걸린 게이트**: B2-STROKE FAIL — 11번(compare) 슬라이드 오른쪽 그림의 X 표시를 `<line class="v-warn">` 두 개로 그렸는데, `v-warn`이 채움 전용 클래스라 선에 쓰면 선이 사라짐(새로 추가된 규칙).
  **고친 방법**: `<line>` 두 개를 `<rect rx>` 두 개(45°/-45° 회전)로 바꿔 X 모양을 **채움 도형**으로 다시 그림(`class="v-warn"` 그대로, 이제 fill로 사용). 1회 build→gate로 ALL PASS.
- **육안 검수 추가 개선**: 슬라이드 6(지도 범위)과 10(영역 단면도)의 삽화가 viewBox에 비해 작게 그려져 있었음(게이트는 이미 통과했지만 육안으로 작고 흐릿함).
  - 슬라이드 6: 육각형(우리나라)이 격자 상자(북위·동경 범위) 안에서 작게 떠 있던 것을, 격자 네 변에 꼭짓점이 닿도록 크게 다시 그림(같은 viewBox, 도형만 확대) — 범위를 시각적으로도 꽉 채워 보여줌.
  - 슬라이드 10: 하늘(반원)·땅(사다리꼴)·바다(띠)로 이루어진 단면도가 viewBox 상단 40%가 빈 채로 작게 그려져 있던 것을, viewBox를 `700×620`→`700×450`로 그림에 맞게 좁히고 사다리꼴(영토)도 확대해 여백을 없앰.
  두 슬라이드 모두 재build 후에도 ALL PASS 유지, 스크린샷으로 확대·여백 축소 확인함. 스킬 파일은 수정하지 않았습니다.

## 성취기준·사실 관련 타협
- 교사가 성취기준 원문을 주지 않아 `standards[0].code`는 `""`, `verified:false`로 두었습니다(스킬 규칙대로 코드를 지어내지 않음). `text`는 공식 원문이 아니라 이번 차시 목표를 요약한 자체 초안이며, 지도안에 "성취기준 원문 확인 필요"로 표시됩니다 — 교사가 원문을 주면 교체해야 합니다.
- 처음에는 "대한 해협 3해리" 예외를 확신이 낮아 뺐으나, 7차 라운드에서 f2·10번 노트에 넣었다(기준선의 통상·직선 기선 설명과 함께). 12해리는 "기준선으로부터"로 적는다.

## 10차 라운드 (슬라이드 9 낱말 "기준선(기선)" 추가 — 독립 재검토)
9번 슬라이드(낱말) 4번째 term을 "기준선"에서 "기준선(기선)"으로 바꿈 — 학생 화면에 기준선이 처음 등장하는 지점(1~8번 슬라이드에는 없음, facts[f2]는 소스 메타데이터라 미렌더)에 교과서 공식 용어 "기선"을 괄호로 한 번만 병기하는 취지. 이후 10번 노트·13번 활동지 문항 3은 계속 "기준선"만 사용해 변경 범위가 9번 term 한 곳으로 한정됨.

| 슬라이드 | 그림이 주장하는 것 | 글이 주장하는 것 | 판정 | 근거 |
|---|---|---|---|---|
| 9 | (그림 없음, 2×2 텍스트 그리드) | 기준선(기선) = 영해를 재는 기준선(썰물 때 해안선이나 바깥 섬들을 이은 직선) | 맞음 | lesson.json 전체에서 "기선" 문자열은 이 한 곳뿐이고 "기준선"의 학생 첫 등장도 9번임(grep 확인). 기선(基線)은 영해 측정 기준선의 법률 용어, 기준선은 초5용 풀어쓴 말로 사실관계 일치. slide-09.png 캡처에서 오른쪽 아래 칸에 한 줄로 자연스럽게 들어가 겹침·넘침 없음 |

gate.mjs 실행 결과 19/20 통과(R1-STALE만 기대된 실패, 검토 후 --review-stamp로 해소).
