# 릴리스 노트 — round 8 ACCEPT

이 문서는 게이트 ALL PASS + `review.json`(별도 검토자 도메인 검토) 통과 뒤에도 남은, **의도적으로 고치지 않은** 사소한 항목을 기록합니다. 스코프 프리즈 정책에 따라 "틀림" 판정·안전 문제·프라이버시 유출·게이트 FAIL만 출시를 막고, 그 외 "애매"·사소한 지적은 모두 여기 기록만 하고 다음 라운드로 넘깁니다.

## 고정 팔레트 (2026-09-26)
- 교과별 강조색을 없애고 모든 교과가 같은 고정 팔레트를 씁니다: primary `#3B1E54`(제목·핵심 낱말·주요 축), secondary `#7A5C9E`(소제목·아이콘·2순위 막대), 코럴 `#FF6F61`(배지·형광펜 밑줄·핵심 점/막대 — 채움 전용, 종이 위 글자색으로 쓰지 않음), 본문 `#191520`, 슬라이드 종이 `#F8F6FA`. 인쇄물은 흰 종이를 그대로 씁니다.
- `meta.accent`와 설계서의 강조색(`style.accentColor`)은 더 이상 쓰지 않습니다 — 남아 있으면 빌드가 경고 한 줄을 남기고 무시합니다.
- 지도 색(땅·바다·하늘)과 맞음/주의 색은 뜻이 있는 색이라 그대로 둡니다.
- 도식 캐시 버전을 올려 모든 예시의 `diagrams/` SVG를 새 팔레트로 다시 컴파일해 넣었습니다(TeX가 없는 PC에서도 같은 그림). README 캡처도 새로 찍었습니다.
- season 삽화 3장은 새 팔레트 색 힌트로 다시 생성했습니다.
- (알려진 문제) photoelectric·photosynthesis의 사진형 이미지는 예전 색 힌트로 만든 자연광 사진 그대로입니다 — `images.mjs --compile-only`로 프롬프트 해시만 새 팔레트 힌트로 옮겼고 다시 생성하지 않았습니다. 새 팔레트로 다시 만들려면 `node scripts/images.mjs <lesson.json> --only <id>`.

## season (`examples/season`)
- (이미지 season-step3-orbit, 애매) 스텝 3 사진("네 곳으로 공전")에서 지구본이 분필 원 위가 아니라 원 안쪽에 살짝 들어가 있습니다 — 실험 결과(남중고도 비교)에는 영향이 없습니다.
- 지도안과 일부 fact(f4)가 여전히 "전등"이라고 쓰고, 슬라이드는 "손전등"으로 통일돼 있습니다.
- `compare` 기본 템플릿이 오른쪽(생각 B) 라벨의 밑줄만 코럴로 칠합니다(글자색은 양쪽 모두 primary, 내용은 손대지 않음, 템플릿 동작).
- 새로 추가한 노트 한 줄이 "…맞춘다"로 끝나 나머지 합쇼체와 살짝 다릅니다.
- 탐구 2(모둠 실험)의 8회 측정을 10분 안에 마치기엔 빠듯합니다 — 노트에 사전 준비 안내를 추가했지만 시간표 자체는 그대로입니다.
- 이 폴더에는 `retro.md`가 없습니다(SKILL.md 기준 예시라 처음부터 회고 기록 없이 시작).

## photoelectric (`examples/photoelectric`)
- 슬라이드 10 그래프에서 "5"와 "−0.5" 눈금 라벨이 선과 겹쳐 안 보입니다(원점 O는 정확한 위치(f=0, Eₖ=0)에 있고, 예측선이 y절편 −2.3(=−W)까지 그려져 읽는 데는 지장이 없습니다).
- 지도안 평가 기준 문구가 "기각되는지"라고 쓰여 슬라이드 7의 "지지하는" 발문과 다른 표현을 씁니다 — 같은 판정의 반대쪽 표현일 뿐 모순은 아닙니다.
- `pe-step2` 사진에서 학생이 계기판이 아니라 정면을 보고 있고, 계기 눈금에 숫자 "0"이 인쇄돼 있지 않습니다(가운데 긴 눈금으로 대신 표시).
- 슬라이드 12의 축소 썸네일에서 보안경이 얼굴 위쪽에 살짝 가려 보입니다 — 원본 이미지에서는 완전히 보입니다.
- (슬라이드 12·이미지 pe-step3, 애매) 정지 전압 측정 절차 사진 3장이 서로 다른 장치처럼 보입니다 — 1단계는 막대형 램프+원판 두 개짜리 긴 관, 2단계는 스포트 램프+사각판 짧은 관, 3단계는 램프 없이 관 전체가 방전관처럼 빛나는 모습입니다. 계기 바늘의 물리적 흐름(조사→오른쪽 편향→중앙 복귀→정확히 0)과 안전 표시(보안경·거리·"직접 보지 않음")는 본문·notes와 맞지만, 세 사진이 한 실험의 연속 절차로는 안 읽힙니다. 슬라이드 11의 정밀 회로 도해가 배선은 그대로 보증합니다.
- S2 경고: 슬라이드 6·7이 글자 수 한계(180자)에 근접(175·164자).
- 성취기준이 아직 `(교사 입력 필요)` 플레이스홀더입니다(원문 미제공).

## photosynthesis (`examples/photosynthesis`)
- 활동지 배지 "활동지 2번"이 항목 번호 "②"로 오해될 여지가 있습니다.
- 슬라이드 14 노트가 나가기 질문을 활동지에 쓰라고 안내하지만, 활동지에 그 칸이 따로 없습니다.
- 활동지 ⑦ 선택지 문구에 "…에서 …에서"처럼 조사가 겹칩니다.
- 지도안 도입 행이 목표 제시보다 실험을 먼저 배치했는데, 실제 슬라이드 순서는 목표가 먼저입니다.
- 활동 1단계는 "살펴본다"라고만 쓰고 활동지 ③은 "1분 동안" 세라고 요구해 표현이 살짝 다릅니다.
- 반 헬몬트 삽화 속 의상이 17세기가 아니라 후대(약 1600년대 말~1700년대) 스타일입니다.
- S2 경고: 슬라이드 4가 160자 한계 중 147자. S6 경고: 성취기준 플레이스홀더.

## argument-essay (`examples/argument-essay`)
- 훅 슬라이드의 "Phones break focus."가 주장을 다시 진술하지 않아 대비가 살짝 약합니다(선택적 개선 사항).
- 정답지가 활동지 표 3·4행에서 "반론과 재반박"을 합쳐 쓴 답도 인정된다는 점을 명시하지 않습니다.
- 결론이 "in classrooms"라고, 주장은 "during class time"이라고 써 표현은 다르지만 문맥상 같은 뜻입니다.

## territory (`examples/territory`)
- 활동지 백지도가 작아(약 26×48mm, 영해 띠 약 1.3mm) 색칠하기 까다롭습니다.
- 12해리 안내 문구 자체에는 대한해협 3해리 예외가 안 적혀 있습니다(사실 f2와 슬라이드 10 노트에는 명시돼 있음).

## linear-function (`examples/linear-function`)
- 그래프 y축 라벨이 "거리"로만 돼 있어 조금 더 구체적일 수 있습니다.
- 활동지 좌표평면 격자에 "-1" 라벨이 빠져 있습니다.
- 슬라이드 9 2단계 삽화에 "1" 이동 라벨이 없습니다(본문 텍스트에는 있음).

## 손대지 않은 이유
위 항목은 모두 각 예시의 별도 검토자가 "애매" 또는 경고 수준으로 남긴 것으로, 학습 목표·정답·안전에는 영향이 없습니다. 스코프 프리즈 정책(라운드가 끝없이 늘어나는 것을 막기 위함)에 따라 이번 라운드에서는 고치지 않고 기록만 남깁니다.
