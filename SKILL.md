---
name: classforge
description: 학교 수업 자료 제작 스킬 — 수업 주제 하나로 교실 프로젝터용 HTML 슬라이드, A4 학생 활동지와 정답지 PDF, 교수·학습 과정안(지도안) PDF를 함께 만들고 20~21개 자동 품질 게이트(KoPub을 쓰면 미설치 PC 대체 배치 검사 B9 추가)와 별도 검토자의 도메인 검토(R1), 캡처 육안 검수를 통과시킨다. 교사가 채운 수업 설계서(.md)나 생성 이미지 삽화도 반영한다. "수업 자료 만들어줘", "수업 PPT", "수업 슬라이드", "학습지/활동지 만들어줘", "지도안/교수학습 과정안", "N학년 OO 단원 N차시 수업 준비", "형성평가 문항", "공개수업 자료" 같은 요청에 사용한다. 초·중·고 전 교과 대상.
---

# classforge — 수업 자료 한 벌 만들기

수업 주제 하나를 **lesson.json** 한 파일로 설계하면, 검증된 템플릿이 네 가지 산출물을 만든다.

| 산출물 | 파일 | 용도 |
|---|---|---|
| 수업 슬라이드 | `out/slides.html` (+`slides.pdf`) | 교실 프로젝터. Pretendard를 파일 안에 넣어 인터넷 없이 어느 PC에서나 열린다(evidence 수업이 KoPub을 쓰면 KoPub이 설치된 PC에서는 KoPub, 없는 PC에서는 Pretendard로 보이며 — 게이트가 두 경우의 배치를 모두 잰다). →/Space 넘김, 단계 공개, 퀴즈 정답 공개, 활동 타이머(T), 발표자 노트(N) |
| 학생 활동지 | `out/worksheet.pdf` | A4 인쇄. 정답 글자는 파일에 아예 들어가지 않는다 |
| 정답지 | `out/answer-key.pdf` | 같은 배치에 정답과 풀이 |
| 교수·학습 과정안 | `out/lesson-plan.pdf` | A4 가로. 도입·전개·정리, 평가 계획, 예상 질문 |

원칙 (강의 「ChatGPT Astra 업무자동화」와 공냥이 공개 스킬들에서 가져옴):
- **목적을 먼저 분명히** — 학습 목표에서 거꾸로 설계한다(목표 → 평가 문항 → 활동 → 슬라이드).
- **내용과 디자인을 분리** — 작성자는 lesson.json의 내용과 슬라이드 유형만 고른다. 레이아웃은 템플릿이 책임진다. 새 레이아웃을 발명하지 않는다.
- **게이트는 물리적 브레이크** — 스스로 "잘 됐다"고 판단하지 말고 `gate.mjs`가 exit 0을 낼 때까지 고친다. 그다음 캡처를 직접 본다.
- **수치는 출처가 있을 때만** — 화면·활동지의 모든 수치는 `facts`에 출처와 함께 있어야 한다(S4 게이트가 대조).
- **끝나면 회고** — 무엇이 게이트에 걸렸고 어떻게 고쳤는지 `retro.md`에 남긴다.

`SKILL_DIR` = 이 파일이 있는 폴더.

## 절차

### 0. 준비 확인 (처음 한 번)
```bash
cd "$SKILL_DIR" && [ -d node_modules/playwright-core ] || npm install
python -c "import fontTools" || pip install fonttools
```
Chrome(또는 Edge)이 필요하다. 다른 경로면 `CLASSFORGE_CHROME` 환경변수로 지정.
`meta.font` 기본은 `"auto"` — evidence 수업은 이 PC에 KoPubWorld Dotum이 설치돼 있으면 자동으로 쓰고(`local()` 참조, 파일은 넣지 않는다), 없으면 Pretendard로 대체하고 경고만 남긴다. 설치는 kopus.org 사용 등록 후 받는다. 자세히는 `references/design.md`의 "글꼴".

정밀 도식(`{tikz}`/`{refraction}`/`{circuit}`/`{particles}`/`{geometry}`/`{vectors}`)과 인라인 수식(`$...$`)은 실제 LaTeX으로 조판한다. **MiKTeX(Windows) 또는 TeX Live(macOS·Linux)를 설치**하면(예: `winget install MiKTeX.MiKTeX`) `build.mjs`가 자동으로 찾아 쓴다(`scripts/diagrams.mjs`의 `findTexBin` — PATH → 흔한 설치 경로 → `CLASSFORGE_TEX_BIN` 환경변수 순으로 찾는다, 설치 안 해도 동작은 하되 도식은 node-tikzjax 오프라인 WASM 대체·수식은 CSS 근사 대체로 품질이 낮아진다). MiKTeX는 처음 쓰는 패키지(circuitikz 등)를 자동 설치하므로(AutoInstall) 한 번은 `initexmf --set-config-value=[MPM]AutoInstall=1`로 켜 둔다 — 그 뒤 첫 컴파일만 패키지를 받느라 느리고(수십 초), 그다음은 도식 하나에 1초 안팎이다. 어느 엔진을 썼는지는 `out/.diagram-engine.json`에 남는다. 자세히는 `references/design.md`의 "정밀 도식은 TikZ로"·"수식 인라인 표기".

### 1. 입력 확인
교사가 수업 설계서를 채워 줬다면(`templates/수업설계서.md` 양식, 전부든 일부든) `intake.mjs`로 먼저 파싱하고 아래 질문은 건너뛴다(`references/intake.md`). 설계서가 없으면 요청에서 다음을 뽑는다. 빠진 것 중 **성취기준과 학교급/학년만** 물어보고 나머지는 합리적으로 정한 뒤 알린다.
- 학교급(`elem`/`middle`/`high`)·학년, 교과, 단원, 차시(예: 4/9차시), 수업 시간(초 40분, 중 45분, 고 50분)
- 성취기준: 교사가 준 원문을 그대로 쓴다. 받지 못했으면 코드는 비우고 `"verified": false`로 둔다. **성취기준 코드도 문장도 지어내지 않는다** — 원문이 없으면 text에 정확히 `"(교사 입력 필요)"`만 쓴다. 지도안에 그대로 표시된다.
- 수업 형태(모둠/짝/개인), 준비물 제약, 참고 자료(교과서 쪽, 기존 PPT·학습지 사진)

작업 폴더 밖의 다른 수업 폴더(이전에 만든 lesson.json·산출물)를 열어 베끼지 않는다 — 교사가 참고 자료로 직접 준 것만 2단계대로 쓴다. 이 스킬의 `examples/`는 스키마 형식 참고용이며 내용·그림을 옮기지 않는다.

### 2. 참고 자료가 있으면 거꾸로 풀기
교사가 기존 자료나 마음에 드는 슬라이드 이미지를 주면, 베끼지 말고 구조만 읽는다: 어떤 순서로 무엇을 보여 주는지 → classforge 슬라이드 유형 중 무엇에 해당하는지 → 그 대응표를 설계에 반영. 이미지·문장을 그대로 옮기지 않는다.

### 3. 수업 설계 (거꾸로 설계)
`references/pedagogy.md`를 따른다. 순서:
1. 학습 목표 1~3개 — 관찰 가능한 동사 + "~할 수 있다."
2. `facts` — 쓸 수치·사실과 출처(교과서, 공공기관). 모르면 수치를 쓰지 않는다.
3. 활동지 문항 — 각 문항에 `obj`(몇 번 목표를 확인하는지). 목표마다 1문항 이상.
4. 지도안 흐름 — 도입·전개·정리, 분 합계 = 수업 시간.
5. 슬라이드 12~16장 — `references/slides.md`의 유형 카탈로그와 권장 흐름.

### 4. lesson.json 작성
작업 폴더에 `<주제-slug>/lesson.json`을 만든다. 스키마는 `references/lesson-schema.md`, 완성 예시는 `examples/season/lesson.json`(초6 과학). **삽화는 세 갈래 규칙으로 고른다**(손으로 쓴 `<svg>`는 레거시 — 게이트 V1-RAWSVG가 잡는다):
- **그래프**(좌표평면·함수·막대그래프, 수치가 정확해야 함) → 선언형 `{plot}`/`{bars}`(`references/design.md` "그래프는 선언형으로").
- **정밀 도형**(광선 추적, 회로, 입자 모형, 작도, 라벨 붙은 구조도 — 정확한 값·위치 관계가 채점·이해에 들어감) → 도해 엔진 `{tikz}`/`{refraction}`/`{circuit}`/`{particles}`/`{geometry}`/`{vectors}`.
- **그 외 모든 삽화**(도입부 훅 장면, 개념 설명 그림, 실험 절차의 실제 도구·손 동작, 활동(`activity`)의 장치 사진, 표지·정리·exit의 분위기 삽화 등 — 정확한 값·라벨이 필요 없는 자리)는 5단계의 생성 이미지 `{image:{...}}`를 쓴다. **개수 제한은 없다** — 수업에 필요한 만큼 넣는다. 약한 SVG 손그림 하나로 이미지를 아끼지 않는다. `visual`은 그 유형이 실제로 그리는 자리에만 쓴다 — 지원하지 않는 자리(예: `quiz`, `compare`/`steps`의 최상위 `visual`)에 쓰면 화면에서 조용히 사라지고 게이트 **S1-VISUAL-IGNORED**가 FAIL시킨다(`references/slides.md`에 유형별 지원 표).

증거 중심 탐구 수업(공식·주장 옆에 근거를 바로 붙이고, 대립 설명은 대칭 배치 후 다음 장에서 판정)으로 설계하려면 `meta.profile: "evidence"`를 쓴다(`references/pedagogy.md` §10, `references/design.md`). evidence 수업은 공식 뒤에 "예측 vs 측정" 검증 슬라이드(plot `data` 시리즈, 값은 facts에 출처와 함께, 예시값이면 "수업 설계(예시 실험값)"으로 명시)를 둔다 — 게이트 E1-VERIFY·E1-SYMBOL·S4-DATA. 측정값은 공식에서 계산한 값이 아니라 실제 측정처럼 편차가 있어야 하고(E1-DATAFIT) 현실에서 나올 수 있는 값이어야 하며, 예시값이면 화면 라벨에도 "(예시)"를 붙인다(E1-EXAMPLE-LABEL). compare 다음 판정 장은 주장이 아니라 실험·관찰·자료로 가른다(`evidence` 필드 또는 data 그래프 — E1-JUDGE-EVIDENCE). evidence 수업의 rival compare(판정 전 대립 설명) 좌우에는 생성 이미지를 쓰지 않는다(V1-COMPAREIMG — 개수·크기를 보장 못해 답을 암시할 수 있다. `{particles}` 등 도해 엔진을 쓴다). **그 compare 바로 다음 판정 슬라이드 자체도 생성 이미지를 쓰지 않는다**(V1-JUDGEIMG — 판정에 쓰는 양(질량·부피 등)을 이미지는 화면에 보장하지 못한다. 예: 저울 사진은 반응 전후 질량이 같은지 보여주지 못한다. `{plot}`/`{bars}`나 도해 엔진을 쓰거나, 시각 자료 없이 `evidence`만 인용하는 quiz 판정으로 대신한다). `diagram`의 캡션이 단위 붙은 수치 결과를 말하는데 이미지를 썼으면 경고가 뜬다(V1-NUMIMG). 회로·전류계·전압계·광전관·검전기·분광기·광학 장치처럼 **구조(배선·연결·기하)가 정답의 일부인 장치**는 사진 한 장으로 끝내지 않는다 — 같은 슬라이드나 인접 슬라이드에 정밀 도해(`{circuit}`/`{tikz}`/`{refraction}`/`{geometry}`)를 반드시 같이 두거나, 캡션에 "도해 참고"를 적어 사진이 분위기용임을 밝힌다(그렇지 않으면 V1-APPARATUS — 일반 수업은 경고, evidence 수업은 FAIL). 실험 절차(steps) 이미지는 앞 단계에서 바뀐 물리 상태(색·모양 변화 등)를 다음 단계 이미지가 그대로 이어받아야 한다 — `image.state`를 단계마다 적으면 `images.mjs`가 자동으로 "앞 단계 상태 → 이 단계 상태"를 프롬프트에 이어 붙인다(`mustShow`/`mustNotShow`로 못 박을 수도 있다 — `references/images.md` "단계 상태 이어가기"). 해요체/합쇼체를 정했으면 캡션·설명 문장도 그 어미로 맺는다(S5-TONE).

### 5. 이미지 요청이 있으면 생성
`visual`에 `{image:{...}}`를 쓴 요청이 있으면 빌드 전에 준비한다(`references/images.md`):
```bash
node "$SKILL_DIR/scripts/images.mjs" <폴더>/lesson.json --compile-only   # 브리프 → 프롬프트 컴파일·검증(공냥 prompt-kit)
node "$SKILL_DIR/scripts/images.mjs" <폴더>/lesson.json                  # 실제 생성(--concurrency N으로 동시성 조절, 기본 2)
```
기본은 subscription 경로(Codex CLI, 사용자 ChatGPT 구독 쿼터, 모델 자동 선택). 특정 모델을 강제하려면 lesson.json에 `"images": {"model": "gpt-image-2.5-sunburst"}`처럼 쓰고 `OPENAI_API_KEY`를 설정하면 그 모델의 API 경로(별도 과금)로 바뀐다. 한 수업의 이미지들이 한 세트처럼 보이게 하려면 `images.style`(팔레트·조명·매체)과, 실험 절차처럼 같은 소재가 이어지면 `images.subject`나 각 이미지의 `continuity`를 쓴다(`references/images.md`). `continuity`나 `images.subject`를 나중에 고치면, 플래그 없이 실행할 때 그 값을 공유하는 **그룹 전체**(같은 steps 슬라이드 또는 같은 continuity)가 함께 다시 만들어진다 — 한 장만 바뀌고 나머지가 옛 설정으로 남는 사고를 막는다(한 장만 콕 집어 다시 만들려면 `--only id`, 그룹의 다른 멤버가 빠지면 경고가 뜬다).

### 6. 빌드 → 게이트 → 수정 반복
```bash
node "$SKILL_DIR/scripts/build.mjs" <폴더>/lesson.json
node "$SKILL_DIR/scripts/gate.mjs"  <폴더>/lesson.json
```
FAIL이 하나라도 있으면 메시지의 `→` 힌트대로 lesson.json을 고치고 다시 빌드·게이트. 템플릿이나 게이트 기준을 느슨하게 바꿔서 통과시키지 않는다. 게이트는 S1~S7(구조·문체·성취기준·설계서 반영)·E1(증거 중심)·I1(이미지)·V1(시각 정책 — 손그림 SVG 금지, 세 갈래 규칙)·B1~B9(브라우저 실측, B9는 KoPub을 쓸 때만)·S8-TRAP(교과별 실험 함정)·R1(7단계 도메인 검토 수행 여부 — 첫 게이트에서는 FAIL이 정상) — 목록과 흔한 해결법은 `references/gates.md`. 설계서(`meta.intake`)가 있으면 S7이, `meta.profile:"evidence"`면 E1이, 이미지 요청이 있으면 I1이, 삽화·도형이 하나라도 있으면(대부분의 수업) V1이 함께 통과해야 한다.

### 7. 캡처 육안 검수 + 도메인 정확성 검토 (게이트 통과 후 필수)
1. `out/shots/overview.png`, 모든 `slide-NN.png`, `worksheet-p*.png`, `answer-key-p*.png`, `lesson-plan-p*.png`를 Read로 열어 `references/design.md`의 **육안 검수 체크리스트**를 확인한다. 걸리면 고치고 6단계부터 다시.
2. **도메인 정확성 검토(필수, 작성자 자신이 아닌 별도 서브에이전트/검증자에게 맡긴다)**: 수치·과학·수학 내용이 있는 수업이면, lesson.json을 쓴 에이전트와 같은 컨텍스트에서 스스로 점검하지 않는다 — 별도로 띄운 서브에이전트(또는 verifier 역할)에게 슬라이드·**활동지·정답지**를 모두 넘겨 검토를 맡긴다. 범위: ① 모든 삽화·그림의 사실·수식 주장이 맞는가(예: 대칭 이중슬릿의 중앙은 밝은 무늬인가, 반사광은 입사 쪽으로 되돌아가는가, 좌표·부호가 본문과 일치하는가), ② **그림과 그 옆 발표자 노트·캡션이 서로 다른 말을 하고 있지 않은가**(figure ↔ notes/caption 일치), ③ **수치가 실제로 가능한 값인가**(대기압·실험 기구의 범위·단위·유효숫자 — 예: 손으로 누르는 주사기가 대기압보다 낮은 압력을 보일 수는 없다), ④ **"측정값"이 공식을 그대로 옮긴 값은 아닌가**(편차 0이면 측정이 아니다), ⑤ **compare 그림이 각 설명의 글과 맞는가**(크기·개수·부피 등 — "입자 크기는 그대로"라는 쪽 그림의 입자가 더 크게 그려져 있지 않은가), ⑥ **노트가 말하는 색·기호·위치가 그림과 같은가**("빨간 입자"라 했는데 그림은 청록색 등), ⑦ **판정 근거가 두 설명을 가르는가** — "기각된 쪽 설명도 이 관찰을 예측하는가?"를 반드시 묻는다. 예측한다면 근거가 아니다(예: "기체는 쉽게 압축된다"는 "입자가 작아진다"와 "입자 사이가 좁아진다" 둘 다 예측하므로 판정 근거가 못 된다), ⑧ **학생 실험 순서** — 판정 장에서 결과를 먼저 보여 준 뒤 같은 실험을 시키면 답을 아는 재현이 된다. compare 슬라이드가 있으면 좌우 그림마다 **용기 크기·입자 크기·입자 개수(또는 그 그림의 핵심 양)를 숫자로 적은 표**를 따로 만들고, 각 칸의 글이 주장하는 것과 대조한다(예: "압축 뒤"를 그린다면 두 칸 모두 압축 전보다 작은 용기여야 하고, "입자 크기는 그대로"인 칸의 입자는 압축 전 그림의 입자와 같은 크기여야 한다). 검토 결과는 **검토 서브에이전트가 `<폴더>/review.json`에 직접 쓴다**(스키마: `references/review-schema.md` — 체크리스트 ①~⑯ 각 항목, 슬라이드별 행, 이미지별 행, 실험 타당성 행). 게이트 **R1-REVIEW**가 이 파일을 검사한다: 파일이 없거나, 빠진 슬라이드·이미지가 있거나, "틀림"이 있거나, lesson.json·슬라이드·이미지보다 오래된 검토면 FAIL이다 — 그래서 6단계 첫 게이트에서는 R1만 남는 것이 정상이고, **7단계 검토 뒤 게이트를 다시 돌려 R1까지 통과해야 완료**다. 고친 뒤에는 검토도 다시 한다 — 검토를 마쳤으면 마지막 단계로 `node scripts/gate.mjs <lesson.json> --review-stamp`를 실행해 review.json에 지금 lesson.json·참조 이미지의 해시(`stamp`)를 찍는다(시각이 아니라 내용으로 신선함을 판정하므로, 그 뒤 내용이 하나라도 바뀌면 stamp가 안 맞아 R1-STALE, 아예 안 찍었으면 R1-STAMP로 FAIL한다). 사람이 읽을 요약은 슬라이드마다 `슬라이드 | 그림이 주장하는 것 | 글(캡션·노트)이 주장하는 것 | 판정(맞음/틀림/애매) | 근거` 표로 `retro.md`에도 남긴다 — "그림이 주장하는 것" 열을 비우지 않는다(그림을 실제로 보고 적어야 ⑤⑥이 걸린다). **생성 이미지는 별도 표를 하나 더 만든다**: `이미지 | 그 이미지가 보여 주는 물리적 상태(색·연결·위치·단계) | 캡션·절차가 요구하는 상태 | 앞 단계에서 이어받아야 할 상태 | 판정`. ⑨ 연속 절차 이미지는 앞 단계의 결과 상태를 이어받는가(탈색한 잎 다음 단계에서 잎이 다시 초록이면 틀림, 빛 가리기는 식물에 붙은 잎에 한다), ⑩ 기구 구조가 맞는가(광전관·검전기·회로 배선 — 사진으로 연결을 검증할 수 없으면 옆에 정밀 도해가 있어야 한다), ⑪ 입자(전자·광자)를 빛나는 공처럼 그려 오개념을 부르지 않는가, ⑫ **안전** — 가열·인화성·전기 실험의 사진과 절차에 안전 수칙이 보이는가(에탄올은 물중탕, 화기 주의), ⑬ 판정할 관찰 결과가 compare **앞** 슬라이드에서 먼저 드러나지 않는가(스포일러), ⑭ **실험 타당성** — 활동·시범 실험이 수업 시간 안에 실제로 그 결과를 낼 수 있는가(전처리·시간 규모·조건: 녹말 대조는 하루 이상 어둠 처리한 탈녹말 식물이 필요하고 10분 어둠으로는 차이가 나지 않는다 등 `references/pedagogy.md` §11 "교과별 실험 함정"), 대조군이 정말 차이를 만드는가, 정답지가 실제로 관찰될 결과를 적었는가, ⑮ **선택지 검산** — 선택형 문항의 오답이 모두 명백히 오답인가(범주어 함정: "필요한 물질이 아닌 것"에 빛에너지도 물질이 아니면 정답이 둘), ⑯ **도해 회로 검산** — 도해를 확대해 모든 선이 끊김 없이 이어지는가, 전지 극성(긴 판 +)과 전극 라벨(음극·양극)이 맞는가, 계기 원 안에 A·V가 있는가. 분위기용 장식 사진(개념·활동과 무관한 이미지)은 표지를 빼고 모두 틀림으로 본다. 작성자가 아닌 사람(교사·검토자)이 수정을 요청해 반영했더라도 그 수정분도 이 체크리스트로 다시 검토한다. 게이트는 도형이 라벨과 겹치는지는 잡아도(B2) 그 안의 물리·수학이 맞는지는 모른다 — 이 검토가 그 자리를 메운다. 틀림이 하나라도 있으면 고치고 6단계부터 다시.

### 8. 전달
- 산출물 경로 4개와 사용법(슬라이드는 Chrome으로 열고 F 전체화면, →/Space 넘김, N 노트, T 타이머)
- 교사가 확인해야 할 것: `verified:false` 성취기준, `facts`의 출처, 교과서 표현과의 차이
- 수정 요청은 lesson.json만 고쳐 다시 빌드하면 된다는 점
- 설계서를 받았다면: 채운 항목 중 반영된 것/AI가 정한 것, `intake.mjs`의 경고, `--check`(S7)에서 `n/a`로 남아 사람이 마지막으로 확인해야 할 항목(`references/intake.md` 5절)

### 9. 회고
`<폴더>/retro.md`에 3~5줄: 걸린 게이트, 원인, 고친 방법, 다음에 처음부터 다르게 할 것.

## 합격 기준 (모두 예)
- [ ] `gate.mjs`가 `ALL PASS`(exit 0)
- [ ] 모든 슬라이드 캡처에서 글자 잘림·겹침·빈 화면 없음, 삽화 라벨이 읽힘
- [ ] 활동지에 정답 흔적 없음, 정답지에 모든 답과 풀이
- [ ] 지도안 시간 합 = 수업 시간, 성취기준 확인 표시가 사실대로
- [ ] 화면·활동지의 모든 수치가 `facts`에 출처와 함께 있음

## 참조
- `references/lesson-schema.md` — lesson.json 전체 스키마, 필드별 한도
- `references/slides.md` — 슬라이드 15유형 카탈로그, 수업 흐름 템플릿
- `references/design.md` — 디자인 규칙, SVG 삽화 작성법, 육안 검수 체크리스트
- `references/pedagogy.md` — 목표·발문·형성평가·활동지·지도안 작성 규칙
- `references/gates.md` — 게이트 18종(+KoPub 사용 시 B9), 오류 코드별 해결법
- `references/intake.md` — 수업 설계서(.md) 반영 절차, intake.json 매핑표, S7 게이트
- `references/images.md` — 이미지 브리프 → 프롬프트 컴파일 → 생성 절차, I1 게이트
- `CREDITS.md` — 참고한 오픈소스와 글꼴 라이선스
